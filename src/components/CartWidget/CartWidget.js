import cart from "./assets/cart.svg";

const CartWitget = () => {
  return (
    <div>
      <img src={cart} alt="cart-wdiget" className="cart-widget" /> 0
    </div>
  );
};

export default CartWitget;
